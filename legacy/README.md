# Legacy experiment scripts

These files are older development scripts kept for transparency. They were useful while building and checking the pipeline, but they are not the final preprocessing or evaluation path used for the main reported v11 ensemble.

For the main PPG-only dissertation result, use `preprocess_mimic2_v4.py`, `train.py`, `run_ensemble.py`, and `evaluate.py` from the repository root.
